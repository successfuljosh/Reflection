<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:Reflections.Model"
             x:Class="Reflections.Views.ReflectionListPage">
    
    <ContentPage.Content>

        <StackLayout Padding="10">
            <SearchBar x:Name="searchBar" Placeholder="Search for reflections here" 
                   SearchButtonPressed="SearchBar_SearchButtonPressed" TextChanged="SearchBar_TextChanged"/>
            <!--<Label Text="Today's Reflections" FontSize="Large" FontAttributes="Bold"
                HorizontalOptions="CenterAndExpand" />-->
            <ListView x:Name="ReflectionListView" IsPullToRefreshEnabled="True" Refreshing="ReflectionListView_Refreshing"
                      ItemSelected="ReflectionListView_ItemSelected"
                      SeparatorColor="Bisque" RowHeight="110" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <StackLayout Orientation="Vertical" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand">
                                    <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="Medium"/>
                                    <Label Text="{Binding Author_Date}" FontSize="Small" FontAttributes="Italic"/>
                                </StackLayout>
                                <Image WidthRequest="100" HeightRequest="100" Source="{Binding ImagesUrl}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            <local:AdMobView AdUnitId="{Binding AdUnitId}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>